# promptname_b

Caveman-full compressed counterpart to `promptname_a`.

Intent:

- same behavior as `promptname_a`;
- system prompt prose compressed using Caveman full strategy;
- user-facing OPPi output style preserved, not cavemanized.

Runtime entrypoint: `main-system-append.md`, selectable with `/prompt-variant promptname_b`.
