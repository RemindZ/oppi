{
  "name": "oppi-monorepo",
  "version": "0.2.9",
  "private": true,
  "type": "module",
  "description": "OPPi monorepo: opinionated Pi-powered coding agent.",
  "license": "MIT",
  "packageManager": "pnpm@10.0.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RemindZ/oppi.git"
  },
  "bugs": {
    "url": "https://github.com/RemindZ/oppi/issues"
  },
  "homepage": "https://github.com/RemindZ/oppi#readme",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "pnpm -r --if-present check",
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "setup:references": "node scripts/setup-references.mjs",
    "setup:feedback-github": "node scripts/setup-feedback-github.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
